# Sentinel Antivirus — macOS Dependencies
# Core detection engines (cross-platform)
psutil>=5.9
watchdog>=3.0
yara-python>=4.3
scikit-learn>=1.3
pyyaml>=6.0
requests>=2.31
pefile>=2023.2.7
numpy>=1.24
lightgbm>=4.0
pyarrow>=14.0
Pillow>=10.0

# macOS GUI (tkinter comes with Homebrew Python or ActivePython)
# Install via: brew install python-tk@3.12

# macOS system tray (pystray works on macOS via AppKit)
pystray>=0.19

# Build tools
pyinstaller>=6.0.0
