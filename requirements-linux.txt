# Sentinel Antivirus — Linux Dependencies
# Core detection engines (cross-platform)
psutil>=5.9
watchdog>=3.0
yara-python>=4.3
scikit-learn>=1.3
pyyaml>=6.0
requests>=2.31
pefile>=2023.2.7
numpy>=1.24
lightgbm>=4.0
pyarrow>=14.0
Pillow>=10.0

# Linux GUI (tkinter comes with python3-tk on most distros)
# Install via: sudo apt install python3-tk  (Debian/Ubuntu)
#              sudo dnf install python3-tkinter  (Fedora/RHEL)

# Optional: Linux Audit integration
# systemd-python>=235  # for native journald access
# python-audit          # for native audit access (via auditd)

# Build tools
pyinstaller>=6.0.0
